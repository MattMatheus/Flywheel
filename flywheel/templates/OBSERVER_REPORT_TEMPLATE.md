# Observer Report: <cycle-id>

The JSON trace sidecar is the canonical machine-readable record; this markdown
report is the human projection. Keep it to what a reviewer needs.

## Metadata
- `cycle_id`:
- `generated_at_utc`:
- `branch`:
- `story_path`:
- `trace_path`:

## Objective
- `intended_outcome`:
- `scope_boundary`:

## Changes Made
- `files_changed`:
- `state_transitions`:

## Validation
- `checks_run`:
- `results`:
- `checks_not_run`:

## Warnings And Risks
- `unresolved_risks`:
- `assumptions_carried`:

## Action Record
- `highest_action_class`:
- `approval_reference`:

## Next Step
- `recommended_next_state`:
- `follow_up_work`:
