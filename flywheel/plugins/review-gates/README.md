# Review Gates Plugin

Example Flywheel plugin that contributes reusable review guidance.

This plugin is intentionally small. It demonstrates how a plugin declares optional guidance without changing Flywheel core behavior.

Validate it with:

```bash
./flywheel/tools/flywheel_plugins.sh doctor
```

