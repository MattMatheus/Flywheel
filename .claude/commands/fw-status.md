---
description: Show Flywheel workflow status
---

Run `./fw status` and summarize the result for the operator: current branch and whether it matches the required branch, workflow state validation, lane counts per domain, and the top active story. Recommend the next stage to launch based on where work currently sits.
