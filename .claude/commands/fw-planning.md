---
description: Run the Flywheel planning stage
---

Run `./fw launch planning --format json`, read the prompt file it points at, and execute the contract: capture goals, constraints, risks, and assumptions; create intake artifacts from the configured templates; recommend the next stage. Do not implement production changes.

Planning context from the operator: $ARGUMENTS
